import {
  noop,
  replaceWxml
} from "./chunk-BNLHSQB7.js";

// src/cva.ts
import { cva as _cva } from "class-variance-authority";
function create(options) {
  const e = options?.disableEscape ? noop : replaceWxml;
  function cva2(base, config) {
    const fn = _cva(base, config);
    return (...props) => {
      return e(fn(...props));
    };
  }
  return {
    cva: cva2
  };
}
var { cva } = create();
export {
  create,
  cva
};
